const discord = require("discord.js");

module.exports.run = async(client, message, args) =>{

    var embed = new discord.MessageEmbed()
    .addField("Je bent verantwoordelijk voor je eigen account. (Dit wilt zeggen dat excuses met het was mijn broer, zusje, kat, hond, geest geen geldig excuus zijn.)");
    }

module.exports.help = {
    name: "regels"
}